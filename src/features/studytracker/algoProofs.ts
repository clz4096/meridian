export const ALGO_PROOFS: Record<string, string> = {
  'binary-search': "Model any comparison-based search of a sorted array of length n as a ternary/binary decision tree whose internal nodes are comparisons against a[mid] and whose leaves are the n+1 possible answers (n hit positions plus 'not found', or the n+1 gaps for a predecessor query). Every distinct outcome must reach a distinct leaf, so the tree has at least n+1 leaves; a binary tree of height h has at most 2^h leaves, forcing h >= ceil(log2(n+1)). Since the number of comparisons on the worst-case input equals the length of the longest root-to-leaf path, no comparison search can beat Omega(log n), and binary search meets it. Sharpen the exercise: run the adversary who, at each comparison, keeps whichever of the two subranges still contains at least half the surviving candidates, and show it survives at least ceil(log2(n+1)) rounds against any querier.",
  'two-pointers': "Consider the sorted-array pair problem: given a[0..n-1] ascending, decide whether some i<j has a[i]+a[j]=T, with pointers l=0, r=n-1. Prove the invariant that no discarded index can belong to a valid pair: if a[l]+a[r] > T then for every k>l we have a[k]+a[r] >= a[l]+a[r] > T, so a[r] cannot pair with any surviving-or-future left index and is safely dropped (symmetrically a[l] when the sum is < T). By induction on the shrinking window l..r, the unique feasibility of any solution pair is preserved until the pointers meet, so completeness holds. Then argue sortedness is exactly what powers the monotonicity a[k] >= a[l]: on an unsorted array the discard step has no such guarantee and the algorithm is unsound — construct a 3-element counterexample where the eliminated element was the answer.",
  'sliding-window': "Prove the two-pointer / monotonic-deque window runs in O(n) via amortized analysis, not by counting nested loops. Take the potential Phi = (number of elements currently in the window or deque); each of the n outer steps advances r exactly once (one enqueue, +1 to a running charge), and the inner while-loop only advances l or pops the deque, each of which permanently removes an index that was inserted exactly once. Assign 2 credits per index at insertion: one pays for its enqueue, one prepays its single eventual removal, so total work is bounded by 2n = O(n) even though a single outer step may do many inner pops. The key lemma to state precisely: l is monotonically non-decreasing and each index enters and leaves the deque at most once, so the summed inner iterations across all outer steps is at most n.",
  'prefix-sums': "Let P[0]=0 and P[k]=a[0]+...+a[k-1], so the sum of a[l..r] telescopes to P[r+1]-P[l] exactly. For the count-of-subarrays-summing-to-K problem, prove the {0:1} seed is not a hack but the statement P[0]=0: a subarray a[l..r] has sum K iff P[r+1]-P[l]=K iff P[l]=P[r+1]-K, so when the scan reaches index r+1 the number of valid left endpoints is precisely the multiplicity of the value P[r+1]-K already recorded in the map — and seeding {0:1} registers the empty prefix P[0]=0 so that prefixes equal to K themselves (l=0) are counted. Prove correctness by induction on r that the map holds exactly the multiset {P[0],...,P[r]} before querying the subarray ending at index r (P[r+1] is inserted only after the query, so l=r+1 / the empty subarray is never counted). Challenge: extend to a 2D prefix table S[i][j] and prove the inclusion-exclusion rectangle formula S[r2+1][c2+1]-S[r1][c2+1]-S[r2+1][c1]+S[r1][c1] by decomposing each corner term as a disjoint union of cells and checking every cell is counted with net coefficient 1 inside the query rectangle and 0 outside.",
  'kadane': "Define best[i] = the maximum sum of a contiguous subarray ending exactly at index i. Prove the recurrence best[i] = a[i] + max(best[i-1], 0) by an exchange argument: an optimal subarray ending at i either is the singleton {i} or extends an optimal subarray ending at i-1 — and it extends iff best[i-1] > 0, since prepending a negative-sum prefix strictly hurts, while any suboptimal prefix could be swapped for the optimal one without lowering the total. Induct on i, then note the answer is max_i best[i]. Challenge: prove why the global maximum must be seeded with a[0] (or -infinity), not 0: on an all-negative array the empty subarray is disallowed, so initializing the running max to 0 wrongly returns 0; show seeding with a[0] is both necessary (to admit the best single element) and sufficient (best[i] never undercounts a mandatory nonempty subarray).",
  'merge-sort': "Prove the Omega(n log n) lower bound for comparison sorting: any deterministic comparison sort is a binary decision tree whose leaves are the permutations it can output, and to sort every input correctly it must have at least n! reachable leaves (one per possible input ordering). A binary tree with n! leaves has height at least log2(n!) = sum_{k=1}^n log2 k >= (n/2) log2(n/2) = Omega(n log n) by Stirling or the crude lower-half bound, and worst-case comparisons equal the tree height. Merge sort makes at most n*ceil(log2 n) comparisons — each of the ceil(log2 n) merge levels does O(n) comparisons total — so it is asymptotically optimal; state and verify the merge step uses at most (size1+size2-1) comparisons to make the constant explicit.",
  'quicksort': "Analyze randomized quicksort by fixing the sorted order z_1 < z_2 < ... < z_n and defining the indicator X_ij = 1 iff z_i and z_j are ever compared. Two elements are compared at most once, and they are compared iff one of z_i, z_j is the first among the set {z_i,...,z_j} to be chosen as a pivot — since if any intermediate z_k is picked first it separates them into different partitions forever. Each of the (j-i+1) elements of that contiguous rank-interval is equally likely to be the first pivot, so Pr[X_ij=1] = 2/(j-i+1), and by linearity E[comparisons] = sum_{i<j} 2/(j-i+1) = sum_{i} 2(H_{n-i+1} - 1) <= 2n H_n = 2n ln n + O(n). Conclude expected O(n log n); note this bound is over the algorithm's coin flips and holds for every input, unlike the deterministic worst case.",
  'union-find': "State Tarjan's theorem: m operations on n elements with union-by-rank and path compression run in O(m * alpha(n)) total, where alpha is the inverse Ackermann function — effectively constant for all realizable n. Prove the weaker but self-contained bound first: with union-by-rank alone, a root of rank r is the root of a subtree of at least 2^r nodes (induct on rank — a rank-r root is formed only by joining two rank-(r-1) roots, each spanning >= 2^{r-1} nodes), so rank <= log2 n and every tree has height O(log n), giving O(log n) per find. Challenge: prove path compression alone (no union-by-rank) still yields O(log n) amortized per operation — set up the accounting where each find that shortens a node's path to the root charges the restructuring against the depth it removes, and bound the total path length created over m operations.",
  'bfs': "Prove BFS from source s computes d[v] = the minimum number of edges on any s-v path. Induct on the distance layer L_k = {v : true distance = k}: the queue invariant is that at any moment it holds vertices of at most two consecutive layers, all of layer k before any of layer k+1 (FIFO order). Show (soundness) that when v is first dequeued and its neighbor w is discovered, d[w] is set to d[v]+1, which is an achievable path length so d[w] >= true distance; and (optimality) that no shorter path can exist, because a path of length < d[v]+1 to w would place w in an earlier layer, contradicting that w was still undiscovered when the layer-d[v] frontier was processed. The two-consecutive-levels invariant is exactly what forbids a vertex from being finalized out of layer order.",
  'dfs': "Prove the white-path theorem: in a DFS forest, v is a descendant of u iff at the moment u is discovered (colored gray) there exists a path from u to v consisting entirely of still-white vertices. (=>) is immediate from the tree structure; (<=) by induction on the white path, using the parenthesis theorem that discovery/finish intervals nest or are disjoint — the next white vertex on the path is discovered during u's open interval and hence becomes a descendant. Then prove the cycle characterization for directed graphs: G has a cycle iff DFS produces a back edge (an edge (u,w) to an ancestor w that is gray when explored). Forward direction: on a cycle, let w be the first vertex discovered; the vertex just before w on the cycle is reached by an all-white path from w so it is a descendant, and its edge back to the still-gray w is a back edge — and conversely any back edge plus tree path closes a cycle.",
  'dijkstra': "Prove Dijkstra's finalization theorem: when a vertex u is extracted from the min-priority queue with key d[u], that key already equals the true shortest-distance delta(s,u). Suppose not; on a true shortest s-u path let y be the first vertex still outside the settled set S and x its predecessor in S. Then d[y] <= d[x] + w(x,y) = delta(s,y) (x was relaxed when settled) and delta(s,y) <= delta(s,u) <= d[u]; combined with d[u] <= d[y] (u was chosen as the minimum) this forces equality throughout, contradiction. Pinpoint the one step that needs non-negativity: delta(s,y) <= delta(s,u) requires that the remaining y..u tail has nonnegative length, so a negative edge breaks it. Challenge: exhibit the failure — with edges s->a=2, s->b=3, b->a=-2, Dijkstra pops a first (2 < 3) and finalizes d[a]=2, but the true shortest distance is s->b->a = 3+(-2) = 1; the single negative edge makes it settle a vertex too early. (It fails precisely when the cheaper direct edge to a is extracted before the improving path through b is relaxed.)",
  'dynamic-programming-lis': "In the patience-sorting LIS algorithm, tails[k] holds the smallest possible tail value of any increasing subsequence of length k+1 seen so far. Prove tails stays strictly sorted: each incoming x overwrites the single leftmost entry >= x (via lower_bound) or appends, and prove by induction this preserves tails[0] < tails[1] < ... — the overwrite can only decrease an entry to a value still greater than its left neighbor (else that neighbor's shorter-subsequence tail would have been >= x too). Prove length(tails) equals the LIS length by a two-way bound: every append extends a genuine increasing subsequence (so LIS >= length), and any increasing subsequence of length L forces tails to have >= L filled slots (so LIS <= length). Challenge: prove that for a strictly increasing LIS you must use lower_bound (first entry >= x, replacing equals) while for a non-decreasing longest subsequence you must use upper_bound (first entry > x), and show a duplicate-valued array where swapping the two gives the wrong length.",
  'topological-sort': "Prove three facts underpinning Kahn's algorithm. (1) Every finite DAG has a source: start anywhere and walk backward along in-edges; since the graph is finite and acyclic the walk cannot repeat a vertex, so it must terminate at a vertex with in-degree 0. (2) Kahn's algorithm — repeatedly emit an in-degree-0 vertex and decrement its successors — emits all V vertices iff the graph is acyclic: if acyclic, the remaining subgraph after each emission is still a DAG and thus still has a source, so the queue never empties early; if there is a cycle, no vertex on it ever reaches in-degree 0 because each has an unremoved in-cycle predecessor. (3) Hence emitted-count < V is a sound and complete cycle test: the leftover vertices with positive in-degree are exactly those trapped in or downstream of a cycle. Prove the emitted order is a valid topological order by noting every edge (u,v) decrements v only after u is emitted, so u precedes v.",
  'greedy-interval-scheduling': "Prove earliest-finish-time-first is optimal for maximum non-overlapping interval selection via a 'greedy stays ahead' exchange argument. Let g_1,g_2,... be the greedy picks (each the compatible interval with the smallest finish time) and o_1,o_2,... any optimal solution both sorted by finish time; prove by induction that finish(g_k) <= finish(o_k) for all k — the base case holds because g_1 has the globally earliest finish, and the step holds because o_{k+1} is compatible with o_k hence with g_k (whose finish is no later), so o_{k+1} was available to greedy, which picked something finishing no later. If optimal had more intervals than greedy, the interval o_{|g|+1} would still be compatible with g_{|g|} and greedy would not have stopped — contradiction, so |g| = |optimal|. Counterexample for shortest-duration-first: intervals [1,10], [9,11], [10,20]; shortest-first grabs [9,11] and blocks both long ones (1 interval), while earliest-finish takes [1,10] and [10,20] (2 intervals).",
};
